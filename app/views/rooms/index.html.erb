<%= content_for :title, "PodLoko - Web iPod Controller" %>
<h1>PodLoko</h1>
<ul>
  <% @rooms.each do |room| %>
      <li>
        <span class="room_id"><%= room.id %></span>
        <a href="<%= room_path(room)%>">
        <span class="room_name"><%= room.name ||"Unnamed" %></span>
    </a>
        <em> - last active: <%= room.last_active.strftime "%H:%M %p on %A" %></em>
      </li>
  <% end %>
</ul>
